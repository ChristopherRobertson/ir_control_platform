# contracts

Canonical shared contracts for the Phase 2 MIRcat + HF2LI golden path.

- Owns: experiment recipes and presets, device capability and status types, validation and readiness types, run state and events, session manifests, and artifact provenance contracts.
- Depends on: no product packages.
- Used by: every other package.
