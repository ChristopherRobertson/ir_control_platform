# Legacy To Target Mapping

The active mapping is now the v1 salvage matrix:

- `docs/refactor/salvage_matrix.md`
- `docs/refactor/deletion_ledger.md`

Legacy UI structure is not a target template. The target workflow is the generic Session / Setup / Results single-wavelength pump-probe slice.
