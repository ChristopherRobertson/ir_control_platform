# Salvage Matrix

The active migration salvage matrix is `docs/refactor/salvage_matrix.md`.

This top-level file is retained only as a pointer so older references do not revive obsolete migration guidance.
